language: node_js
node_js:
- stable
cache:
  directories:
    - "node_modules"

before_script:
  - npm install -g create-react-app

script:
  - npm install
  - npm run test:CI
  - npm run build

install:
  - npm install -g firebase-tools

after_success:
  - firebase deploy --token $FIREBASE_TOKEN